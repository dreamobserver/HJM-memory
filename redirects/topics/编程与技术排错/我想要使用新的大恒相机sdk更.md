# 我想要使用新的大恒相机sdk更

**分类**: 编程与技术排错
**来源**: DeepSeek Chat 导出
**来源平台**: DeepSeek

---

# 用户：

我想要使用新的大恒相机sdk更换旧的大恒相机sdk，旧的sdk为MTUCamera.dll，新的sdk会叫做什么？（在新的sdk文件夹中未找到，原来的名称）

# DeepSeek：

**原始回答**

关于您提出的从旧的 `MTUCamera.dll` 升级到新的大恒相机SDK的问题，根据目前的搜索结果，**我无法直接确认新SDK中与旧版 `MTUCamera.dll` 相对应的具体文件名称**。

不过，这很可能是因为新版SDK在结构和命名上已经发生了重大变化。下面我将为您梳理新SDK的信息，并提供查找确切文件的方法。

### 新版本SDK概览

大恒图像（Daheng Imaging）目前的相机SDK品牌名为 **GalaxySDK**。根据官方发布的信息，其最新版本已迭代至 **V2.4.2507.9231** 。新版本的GalaxySDK在性能和功能上都有显著提升，例如大幅降低了CPU占用率，并提供了对.NET Core等新技术的支持 。

值得注意的是，新版GalaxySDK很可能采用了**模块化的设计**，将功能分散到了多个不同的动态库文件中，而不再像旧版本那样可能将所有功能集成在一个 `MTUCamera.dll` 里 。

### 如何定位核心文件

虽然无法直接给出文件名，但您可以通过以下方法来找到新SDK中的核心库文件：

1.  **查阅官方文档**：这是最可靠的方法。请务必查看您下载的GalaxySDK开发包中附带的文档（通常在 `Documentation` 或 `Help` 文件夹内）。寻找名为《编程指南》、《入门手册》或《API参考》的文档，其中通常会明确指明开发所需引用的核心库文件。
2.  **在SDK文件夹中搜索**：您可以尝试在解压后的SDK文件夹中搜索 `.dll` 或 `.so`（Linux库文件）扩展名。重点关注以下目录：
    *   `Bin`、`Lib`、`Library` 或 `Runtime` 文件夹。
    *   根据您使用的编程语言（如C++、C#），在相应语言的样例工程（`Samples` 文件夹）中，查看项目配置或代码里引用了哪些DLL文件。
3.  **联系官方技术支持**：如果以上方法都无法确定，强烈建议您直接联系大恒图像的官方技术支持。提供您旧SDK的详细版本和您正在使用的新SDK版本，他们能给出最准确的答复。官方客服热线为 **400 999 7595** 。

### 升级注意事项

在更换SDK时，除了找到正确的文件，还需要注意：

*   **接口变更**：新版SDK的API函数名、参数或调用方式很可能与旧版不同 。您需要仔细阅读新版的API文档，并据此修改您的源代码。
*   **版本兼容性**：确保您下载的SDK版本与您的操作系统和开发环境（如Visual Studio版本、.NET Framework版本）兼容 。

希望这些信息能帮助您顺利找到所需的文件并完成SDK的升级。如果您需要进一步了解新版GalaxySDK的功能特性，我很乐意为您提供更多信息。
